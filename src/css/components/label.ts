import { createGlobalStyle } from "styled-components";

export const LabelStyle = createGlobalStyle`
  .frs-label {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    background-color: transparent;
  }
`;
